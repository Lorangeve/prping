# ICMP mock · 客户端配方（client.pktl）：两步发包模拟——配方可包含任意多包。
#
# 步骤 1：发 echo request（id=0x1234, seq=1）→ `wait: 2` 校验 server 配方的
#         echo reply（sniffer 匹配 type=0 + id/seq，seq 带配方标记 1001）→
#         extract reply.icmp.id 写 global.cid。
# 步骤 2：`delay: 0.5` 给 server 配方留出重开下一轮抓包的时间（回环上 client
#         全程 <1ms 就跑完，不加 delay 会在 server 下一轮监听就绪前就错过 req2，
#         导致只命中 3 条）。再发 echo request（seq=2，sniffer 期望 1002）→ `wait: 2` 校验。
#
# 为什么 seq 期望 1001/1002：回环上内核会替答 ICMP echo（macOS/Linux 都回 type=0
# 且原样回显 id/seq/载荷——ping 127.0.0.1 不需要服务端）。纯回显的 reply 与内核
# 替答逐字节相同，client 无法分辨。server 的 reply 把 seq 偏移 +1000 作配方标记
# （见 reply.pkt），sniffer 只认偏移后的值 → 命中的必然是配方回包。
#
# 运行（需 root；与 server.pktl 同机两个终端）：
#   sudo prping packet examples/icmp_mock/client.pktl
#   （目标 127.0.0.1 已内建在 req1/req2 的 ipv4 dst，无需 positional HOST——
#     配方 + 裸 HOST（无端口）会触发「需要目标端口」校验，除非显式 --raw；
#     跨机实验：-p ip=<对端 IP>）
global:
- cid

recipe:
- packet: req1.pkt
  raw: true
  wait: 2
  extract:
  - name: cid
    from: reply.icmp.id
    as: int
- packet: req2.pkt
  raw: true
  wait: 2
  delay: 0.5
