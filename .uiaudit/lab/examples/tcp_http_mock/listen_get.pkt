# server 配方 · 第 2 组步骤 1：链路层持续监听——匹配握手完成后的 HTTP GET 请求。
#
# 为什么 sniffer 能直接写 http(method="GET")——http 层反解分派（eng_lib/headers.pkt
# #[proto(kind="http")] 的两条规则）：
#   #[rule(or(tcp(dport=80), tcp(dport=8080)))]  ← 端口分派：dport=80/8080 的帧才尝试
#   #[rule(contains("HTTP/", in=start_line))]     ← 内容分派兜底：start_line 含 "HTTP/"
# 本目录全程用 80 端口（synack.pkt / http_resp.pkt 的 sport 固定 80 与之配套——
# 这也是本 mock 选 80 而非高位端口的根本原因：不是端口的 http 分派不出 http 层）。
# GET 请求帧 dport=80 → 端口分派命中 → 载荷按 http 反解出 IR 层；请求行
# "GET / HTTP/1.1" 按空格拆为 method="GET" / path="/" / version="HTTP/1.1"。
#
# 为什么不会被内核噪声帧误触发：内核对本 mock 的裸包会替答 RST（见 README
# 「内核 RST 副作用」），RST/ACK 等噪声帧载荷为空、无 http 层——http 子句在反解
# 结果里找不到 http 命中即不匹配，flags/端口再像也进不了这条规则。
#
# 局域网实测提示：该规则同样会命中线上任意主机的 GET（广谱性同 listen_syn.pkt），
# 需要收窄可写 and(match tcp(dport=80), match http(method="GET"))。
p = raw(bytes="")
use(p) |> ipv4()

sniffer:
  - match http(method="GET")
