# client 配方 · 步骤 3：握手完成后发 HTTP GET（PSH|ACK 数据段），wait: 2 校验 200 OK。
#
# seq/ack：seq=0x1001（=cseq+1，与步骤 2 的 ACK 相同——ACK 无载荷不占序号，GET 与它
# 是同一方向连续的段）；ack=global("sseq") + 1（服务端没发过新数据，确认号不变）。
#
# GET 载荷字节数 = 38 = 0x26："GET / HTTP/1.1\r\n"(14+2) + "Host: mock.example\r\n"
# (18+2) + 空行"\r\n"(2)。server 的 http_resp.pkt 用 ack=r2_seq+38 确认整个请求——
# **这个常量两端手工对齐**（改 headers 必须同步两端；教学 mock 不自动算长度；
# Host 头为固定值：headers 不支持动态拼接，真实浏览器会填真实域名）。
#
# sniffer 为什么是 and() 跨层与（sniffer 反解后对整包判定，支持跨层组合；
# #[rule] 是解析期单点分派反而做不到）：
#   * tcp 子句先锁"这是我这条流的数据段"：sport=80、dport=40000（=我方 sport）、
#     flags 字节 PSH|ACK（bor(psh(),ack()) 与 2 字节大端比较；内核噪声 RST 的
#     rst|ack=0x14 ≠ psh|ack=0x18，先被排掉）；
#   * http 子句钉状态行：响应帧 dport=40000 不是 80/8080，http 的**端口分派**不命中，
#     但内容分派兜底（contains("HTTP/", in=start_line)）仍反解出 http 层，且响应行
#     以 "HTTP/" 开头时 method/path=None、version=整行 "HTTP/1.1 200 OK"——
#     version 精确字面量即状态行校验。
#     注：payload 字节谓词 raw(contains("200 OK")) 在此**不可用**——响应载荷已反解
#     成 http 层、不再是 Raw 层（packet --out + engine --pcap 实测）。
#   * 想最小化校验可删掉 http 子句只留 tcp 子句（tcp_handshake_listen 客户端同款）。
# sseq 缺省 0x2000 仅用于独立解析/展示；配方运行时由 client 步骤 1 extract 注入真实 ISN。
get = http(start_line="GET / HTTP/1.1", headers=["Host: mock.example"])
use(get) |> tcp(sport=40000, dport=params("port", 80), seq=0x1001, ack=global("sseq", 0x2000) + 1, flags=bor(psh(), ack()), window=65535) |> ipv4(src=params("sip", "127.0.0.1"), dst=params("ip", "127.0.0.1"))

sniffer:
  - and(match tcp(sport=80, dport=40000, flags=bor(psh(), ack())), match http(version="HTTP/1.1 200 OK"))
