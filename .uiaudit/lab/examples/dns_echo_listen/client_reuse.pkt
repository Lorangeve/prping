# 客户端步骤 2：复用 global.tid 再发查询——tid 来自步骤 1 从应答提取的 dns.id
# （global("tid") 跨步骤复用；独立运行本文件时回退默认值 0x4242）。
# 原 dns_recipe/ 的核心教学点（extract 回包字段 → global → 后续步骤复用）
# 已并入本目录：dns.id 与发包一致由服务端配方回显保证，sniffer 的 id=id
# 引用发包同层同名字段校验应答（SentField）。
q = dns(id=global("tid", 0x4242), questions=["example.com"])
full = use(q) |> udp(dport=53) |> ipv4(dst=params("ip", "127.0.0.1")) |> eth()

export:
- full

sniffer:
  - match dns(id=id, flags=0x8180)
