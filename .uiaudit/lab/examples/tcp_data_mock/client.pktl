# TCP 数据传输 mock · 客户端配方（client.pktl）：两步发包模拟——配方可包含任意多包。
#
# 步骤 1：发数据段（PSH|ACK，seq=0x1000，载荷 45 字节）→ wait: 2 校验 server 配方
#         的纯 ACK（sniffer 匹配 sport=9090 + dport=40000 + flags=ack() +
#         ack=0x102D=seq+45）→ extract reply.tcp.seq 写 global.sseq（服务端序号，
#         步骤 2 的确认号推导用）。内核对未监听端口的 RST 不会误命中（flags 字节
#         0x04≠0x10，见 tcp_data.pkt 注释）。
# 步骤 2：delay: 0.5 给 server 配方留出重开下一轮抓包的时间（回环上 client 亚毫秒
#         就跑完，不加 delay 会在 server 下一轮监听就绪前错过第二个数据段；经验见
#         icmp_mock README「为什么 seq 是 1001/1002」）。再发数据段（seq=0x102D
#         = 上一段 seq+45；ack=global("sseq")+1 = 步骤 1 提取的服务端 seq 推导）
#         → wait: 2 校验第二轮 ACK（ack=0x105A=0x102D+45）。
#
# 为什么 sniffer 全用字面量：wait 步骤的匹配对象是收到的帧，规则里写死期望值
# （端口/标志/确认号）才能逐字段核对 mock 的数值关系；listen 模式（server 侧）
# 更是只允许字面量/值表达式（没有发包可引用）。若用 -p port= 覆盖端口，
# sniffer 的 sport=9090 字面量需同步改。
#
# 运行（需 root；与 server.pktl 同机两个终端）：
#   sudo prping packet examples/tcp_data_mock/client.pktl
global:
- sseq

recipe:
- packet: tcp_data.pkt
  raw: true
  wait: 2
  extract:
  - name: sseq
    from: reply.tcp.seq
    as: int
- packet: tcp_data2.pkt
  raw: true
  wait: 2
  delay: 0.5
