# client 配方 · 步骤 2（delay: 0.5 后发送）：第二个数据段——**双向 seq 推进**教学点。
#
# 与步骤 1 的两处差异（其余字段同 tcp_data.pkt，见那里的标志位表/内核副作用注释）：
#   1. seq=0x102D = 0x1000 + 45：上一个数据段占用了 45 个序号，第二个数据段的
#      seq 从「上一个段 seq + 载荷长度」继续——**发送方 seq 随数据推进**；
#   2. ack=global("sseq") + 1：不再用固定 0x2001，而是用步骤 1 从服务端 ACK 里
#      **extract** 出来的 seq（global.sseq）+1——「确认号 = 期望对方下一个字节」，
#      +1 的含义：sseq 是服务端 ACK 段的 seq，本 mock 服务端只发纯 ACK（无载荷），
#      教学上取 +1 表示「你上一个段我收完了」；真实 TCP 中纯 ACK 不消耗序号
#      （SYN/FIN 各占 1、纯数据按载荷长度计），这里的 +1 是 mock 简化，
#      重点是「确认号应从收到的段头提取/推导，而非拍脑袋固定」——步骤 1 用固定值
#      建立数值直觉，步骤 2 用 extract 建立动态推导，两步对照正是本目录的设计。
#
# 第二轮确认：服务端对第二个数据段的 ACK ack = 0x102D + 45 = 0x105A
# （= 累计字节数 0x1000 + 90），sniffer 里逐字段核对——**累积确认**：0x105A 之前
# 的两段数据（0x1000..0x102D、0x102D..0x105A）全部确认，无需逐段 ACK。
#
# global("sseq", 0x2000) 带默认值：裸 `prping engine` 分析无配方上下文，sseq
# 未设置会报错——默认值取第一轮 extract 的期望示例（0x2000）；配方运行时步骤 1
# 的 extract 已写 sseq，默认值不生效（延迟求值，见 tcp_ack.pkt 注释）。
#
# 教学取舍：seq=0x102D 写成固定字面量而非 global("sseq") 推导（client 的发送方
# seq 本地自增，无需从回包提取）；ack 用 extract 的 sseq 动态构造（接收方状态
# 必须来自对端）。固定值便于教学对照，真实场景应动态提取/随机化。
data = raw(bytes="Hello, TCP! This is a data transmission test.")
use(data) |> tcp(sport=40000, dport=params("port", 9090), seq=0x102D, ack=global("sseq", 0x2000) + 1, flags=bor(psh(), ack()), window=65535) |> ipv4(src="127.0.0.1", dst=params("ip", "127.0.0.1"))

sniffer:
  - match tcp(sport=9090, dport=40000, flags=ack(), ack=0x105A)
