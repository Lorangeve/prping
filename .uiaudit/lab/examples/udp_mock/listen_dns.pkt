# server 配方 · 第 1 组步骤 1：持续监听（wait: 无值）——本包有 udp 传输层 → 自动选
# **UDP 数据报监听**（绑定 :5533，载荷反解后按 sniffer 匹配），命中后配方继续
# （触发第 2 步发 DNS 应答）。
#
# 为什么绑 :5533 而不是 :53：
#   DNS 的 proto 分派规则是 udp(dport=53)/tcp(dport=53)（eng_lib/headers.pkt），
#   但规则未命中时引擎还有**内容识别回退**——按 dns 声明结构直接试解析：只要
#   四区计数非零（查询报文 qdcount=1）就能反解出 dns 层，**不依赖端口**。UDP
#   数据报监听拿到的是纯载荷（没有端口上下文），反解本来就只看内容——所以
#   监听端可以绑任意高位端口。选 5533 全程免 root（绑 <1024 特权端口需
#   root/管理员，见 README「端口的 root 说明」）。
#
# 这里的包定义仅用于推导监听端口（最外层 udp dport=5533）与文档说明，
# 载荷是空占位（实际匹配完全由 sniffer 规则决定）。
p = raw(bytes="")
use(p) |> udp(dport=5533) |> ipv4(dst="127.0.0.1") |> eth()

# 监听规则：DNS 查询（flags=0x0100 = QR=0 查询位 + RD=1 期望递归）。
# sniffer 规则只能用字面量（监听没有发包可引用，不能写 SentField）。
sniffer:
  - match dns(flags=0x0100)
