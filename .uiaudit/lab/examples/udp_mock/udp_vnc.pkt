# client 配方 · 步骤 2：发 VNC 版本横幅（RFB 握手，raw 字符串载荷——同一套
# UDP 协议栈、不同端口 = 不同应用协议），`wait: 2` 等服务端回横幅。
#
# 载荷："RFB 003.008\n"（12B 定长文本）——RFB 协议魔数 + 版本 3.8 + 换行，
# RFC 6143 §7.1.1 的协议版本横幅（真实 VNC 由服务端先发，见 vnc_banner.pkt
# 的时序说明）。raw(bytes=...) 直喂字节：无字段结构可声明，正是「裸字节
# 载荷走通用传输」的典型（对照第 1 步 dns 的结构化构造）。
# 传输：udp(sport=12346, dport=5900)——端口换了，协议栈不变：**端口即服务
# 分派**（同一主机 5533 跑 DNS mock、5900 跑 VNC mock）。
# 目标地址可注入：-p ip=127.0.0.1（默认本机回环）。
vnc = raw(bytes="RFB 003.008\n")
full = use(vnc) |> udp(sport=12346, dport=5900)
               |> ipv4(src="127.0.0.1", dst=params("ip", "127.0.0.1"))
               |> eth(src_mac="00:11:22:33:44:55", dst_mac="66:77:88:99:aa:bb")

export:
- full

# ── 为什么本步 wait 不配 sniffer ──
# 回包是裸横幅（普通 UDP socket 收到的就是 12B 文本，无 IP/UDP 头），反解引擎
# 对裸载荷只尝试 dns/http，横幅都解析失败 → 一个层都没有，任何 sniffer 规则
# （含 raw(...) 字节谓词）都无从匹配（实测 ignored）。引擎对无 sniffer 的
# wait 的语义是「收到的第一个数据报即应答」——且发送与应答字节相同时，内置
# 的 DNS-id 门（首 2 字节相同）天然通过。这就是 mock 的回包核验：服务端确实
# 回了 12B（命中会打印 ✓ reply 与字节数/RTT）；内容层面的校验由服务端的
# 跨层匹配规则承担（见 listen_vnc.pkt）。
