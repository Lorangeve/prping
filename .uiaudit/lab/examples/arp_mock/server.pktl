# ARP mock · 服务端配方（server.pktl）：一个 pktl 就是一台 ARP 应答机——模拟
# 「持有某个 IP 的主机」的协议栈：收到 ARP 请求，判断归属，单播回一句应答。
#
# 配方结构（一组 listen+reply 对应 client.pktl 的一步；pktl 可包含任意多包）：
#   步骤 1：`wait:`（无值 = 持续监听）——listen.pkt 无 udp/tcp 传输层 → 自动选
#           **链路层监听完整帧**（AF_PACKET 收完整 eth+arp 帧，需 root），sniffer
#           匹配 ARP 请求（op="request"），命中后 extract 匹配帧字段写 global：
#           r_sha=请求方 MAC / r_spa=请求方 IP / r_tpa=被询问的 IP
#           （链路层监听帧内 arp 字段齐全，无需 UDP 监听那种 reply.peer.* 对端
#           回填——目的 MAC 就在帧的 arp.sha 里）。
#   步骤 2：触发发包——arp_reply.pkt 用 global 构造 ARP 应答单播发回请求方。
#           `raw: true`：eth 帧只能 raw 发送完整帧（AF_PACKET 按帧内目的 MAC
#           直发，无需 IP 目标）；ARP 无传输层本会自动回退 raw，显式写更清晰。
#
# 要服务更多次请求，照 icmp_mock 的样子再写几组 listen+reply 即可。本 mock 只
# 服务一轮（client 步骤 1 的请求）；client 步骤 2 的 gratuitous ARP 是纯发送、
# 无 wait 期待，无需配对监听（见 free_arp.pkt 注释）。
#
# 为什么内核不会抢答：本 mock 缺省跑在 Linux 回环 lo 上（raw 发送缺省 iface=lo），
# lo 带 IFF_NOARP、不跑 ARP——内核即使看到 tpa=127.0.0.1 归本机也不替答，因此
# 不需要 icmp_mock 那套「seq+1000 配方标记」来区分内核替答；sniffer 的固定 MAC
# 过滤是为**真实网段**运行时排除真实设备应答的干扰（详见 README）。
#
# 运行（需 root；链路层 AF_PACKET 收发，Linux 原生 / macOS·Win 走 libpcap 路径）：
#   sudo prping packet examples/arp_mock/server.pktl
# 客户端（另一终端，见 client.pktl）：
#   sudo prping packet examples/arp_mock/client.pktl
global:
- r_sha
- r_spa
- r_tpa

recipe:
- packet: listen.pkt
  wait:
  extract:
  - name: r_sha
    from: reply.arp.sha
    as: str
  - name: r_spa
    from: reply.arp.spa
    as: str
  - name: r_tpa
    from: reply.arp.tpa
    as: str
- packet: arp_reply.pkt
  raw: true
