# arp_mock · client 配方 · 步骤 1：发 ARP 请求（广播问「谁是 127.0.0.1」），
# `wait: 2` 校验 mock 服务端的 ARP 应答，并把应答的 sha 提取进 global.rmac。
#
# extract 是真实协议栈的标志性动作——**从应答学习对方的 MAC**（RFC 826 里连被
# 询问者都会顺带把请求方的 spa->sha 记进 ARP 缓存）。本示例只有一步需要 MAC，
# rmac 写进 global 后单步用不到，纯演示 extract 复用入口：后续步骤可引用
# global("rmac") 动态构造（对照旧 link_arp 只能 -p tmac= 人工注入应答 MAC）。
#
# 字段对照（RFC 826 请求格式）：
#   op=request()          操作码 1——"询问"
#   sha="00:11:22:33:44:55"  请求方 MAC（真实场景 = 本机网卡 MAC；固定值便于
#                         教学对照，mock 的正常手段）
#   spa="127.0.0.1"       请求方 IP（真实场景 = 本机接口 IP）
#   tha="00:00:00:00:00:00"  全零——请求时目标 MAC 未知，正是要问的东西
#   tpa=params("ip","127.0.0.1")  要解析的 IP，可注入：-p ip=<邻居 IP> 打真实
#                         网段邻居（缺省回环，见 README「内核不会抢答」）
# eth 层：dst=广播 ff:ff:ff:ff:ff:ff（本地网段谁都可能答——广播是 ARP 的核心
# 语义：一个问句全员可闻，答案只该由持有者给出）；ethertype 由 arp 层自动 0x0806。
#
# sniffer 期望（双重过滤）：
#   ① op="reply"——排除请求帧。lo 上自己发出去的帧自己的监听也能看到（出向+
#      入向双投递，引擎按最近 N 帧去重），请求帧 op="request" 不会误命中；
#   ② sha="66:77:88:99:aa:bb"——只认 mock 服务端固定 MAC。在**真实网段**运行时，
#      真主机若真持有 tpa 会抢答 op="reply"，但其 MAC ≠ mock 约定值 → 不会误
#      命中——与 icmp_mock「回包 seq 偏移 +1000 作配方标记」同一思路：给 mock
#      回包一个真实内核/真机不会产生的特征，确保命中的必然是配方应答。
p = arp(op=request(), sha="00:11:22:33:44:55", spa="127.0.0.1",
        tha="00:00:00:00:00:00", tpa=params("ip", "127.0.0.1"))
use(p) |> eth(src_mac="00:11:22:33:44:55", dst_mac="ff:ff:ff:ff:ff:ff")

sniffer:
  - match arp(op="reply", sha="66:77:88:99:aa:bb")
