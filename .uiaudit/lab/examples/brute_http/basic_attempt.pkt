# HTTP Basic 认证爆破 · 尝试包（payload 模式，免 root）：GET /admin 带
# Authorization 头，候选凭据是**预计算好的 base64 串**经 params 注入。
#
# Basic 认证格式（RFC 7617）：Authorization: Basic base64(user:password)
# base64 离线算好直接进配方（DSL 无 base64 原语）：
#   printf 'admin:secret123' | base64   →  YWRtaW46c2VjcmV0MTIz
# 注意 DSL 的头行列表元素只接受字符串字面量/params（concat 结果是字节，
# 过不了 hdr_line 的 text 类型），所以 params 注入**整行**头——候选表即
# 「完整 Authorization 头」对照表，见 basic.pktl 每步注释。
#
# 命中判定（无 sniffer，见下）：靶场对错误凭据**静默断开**（不回包），本步
# wait 读不到响应 → ✗ 超时失败；正确凭据收到完整 "HTTP/1.1 200 OK" 响应 → ✓。
req = http(start_line="GET /admin HTTP/1.1",
           headers=[params("auth", "Authorization: Basic YWRtaW46YWRtaW4="),
                    "Host: 127.0.0.1:8000",
                    "Connection: close"],
           body="")
use(req) |> tcp(dport=params("port", 8000)) |> ipv4(dst=params("ip", "127.0.0.1"))

# 同 form_attempt.pkt：TCP 载荷模式不做 sniffer 匹配、裸响应反解不出 http 层
# （eth 误判），命中判定退化为「有无响应」（靶场静默失败，见 lab_server.py）。
