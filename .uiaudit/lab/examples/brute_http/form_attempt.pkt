# HTTP 表单登录爆破 · 尝试包（payload 模式，免 root）：引擎提取最外层 tcp 的
# 应用层载荷，经普通 socket 连接 params("ip"):params("port") 发送并读响应。
#
# 为什么凭据放在查询串而不是 POST body：http() 不自动补 Content-Length，
# DSL 也没有 strlen 原语（算不出 body 字节数）——查询串版本不需要 Content-Length，
# 对任意标准 HTTP 服务都成立。POST 变体要固定 body 长度（见 README「变体」）。
#
# ⚠ 候选要**整段注入**（params: q=user=admin&password=XXXX）：步骤 params 的纯
# 数字串会被自动转成整数（pass=123456 → Int），concat 不收整数——注入完整查询段
# （字母开头）保持字符串类型，这是本示例顺手演示的一条引擎类型规则。
#
# 命中判定（无 sniffer，见下）：靶场对错误凭据**静默断开**（不回包），本步
# wait 读不到响应 → ✗ 超时失败（client.pktl 用 on_error: continue 兜住继续下一
# 候选）；正确凭据收到完整 "HTTP/1.1 200 OK" 响应 → ✓，回包 hexdump 里状态行
# 一眼可辨。✗/✓ 的对比就是爆破的「试到命中」过程。
req = http(start_line=concat("GET /login?", params("q", "user=admin&password=????"),
                             " HTTP/1.1"),
           headers=["Host: 127.0.0.1:8000",
                    "User-Agent: prping-brute-lab/0.1",
                    "Connection: close"],
           body="")
use(req) |> tcp(dport=params("port", 8000)) |> ipv4(dst=params("ip", "127.0.0.1"))

# 注意：这里没有 sniffer——TCP 载荷模式的回包是普通 socket 读到的裸应用层字节，
# 引擎不做 sniffer 匹配（UDP 才有，见 src/engine/pkg/send.rs）；裸 HTTP 响应的
# 前两字节还会被裸字节反解误认成以太网帧（第 13-14 字节 "\r\n" 恰好落在
# ethertype 位置且 ≥ 0x0600），extract 也取不到 http 状态行——所以命中判定
# 退化为「有无响应」（靶场静默失败的原因，见 lab_server.py docstring）。
