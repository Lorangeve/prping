# server 配方 · 步骤 1：链路层持续监听（wait: 无值）——匹配纯 SYN。
#
# 监听规则：只响应**纯 SYN**（flags="syn" 精确匹配；SYN-ACK 反解为 "syn,ack"
# 不命中——不会把自己刚回的 SYN-ACK 当新请求）。
# 包体只有 ipv4（无 udp/tcp 传输层）→ 自动选链路层监听完整帧；
# 包定义仅用于选监听方式/文档，实际匹配完全由 sniffer 规则决定。
p = raw(bytes="")
use(p) |> ipv4()

sniffer:
  - match tcp(flags="syn")
