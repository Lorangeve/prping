# pktlang 全面测试计划

## 测试目标
全面测试 pktlang (packet-dsl) 的所有功能，确保语法、解析、语义分析、求值、序列化等环节的正确性。

## 测试范围

### 1. 语法解析测试
- 基础语法：标识符、字符串、数字、十六进制
- 管道语法：`|>` 操作符
- 函数调用：命名参数、位置参数
- 导入导出：`import`、`export`
- 注释和空白处理
- 错误处理和错误恢复

### 2. 类型系统测试
- 数字类型：u8, be16, be32, be64, le16, le32, le64
- 字符串类型
- 字节类型：raw, hex
- 地址类型：ip4, ip6, mac
- 标志位类型：flags
- 校验和/摘要类型

### 3. 值函数和原语测试
- 字节原语：raw(), hex(), u8(), be16(), be32(), le16(), le32()
- 算法原语：count(), cksum(), md5(), sha1(), sha256()
- 位运算：bor(), band(), bxor(), bnot(), shl(), shr()
- 随机值：rand16(), rand8(), rand_bytes()
- 填充：pad()
- 域名解析：dns()
- 字符串模板：tpl()

### 4. 协议层测试
- 链路层：eth, arp
- 网络层：ipv4, ipv6
- 传输层：tcp, udp, icmp
- 应用层：http, dns
- 自定义协议：#[proto] 声明

### 5. 管道和组合测试
- 单层管道：`use(a) |> tcp()`
- 多层管道：`use(a) |> tcp() |> ipv4() |> eth()`
- 多载荷：`use(a, b) |> tcp()`
- 多包导出：`export: - a - b`

### 6. 配方 (Recipe) 测试
- 步骤执行：多步骤配方
- 参数注入：`params()`
- sniffer 匹配
- 错误处理

### 7. 语义分析测试
- 静态检查：类型检查、范围检查
- 诊断信息：错误消息、警告
- 符号解析：导入解析、作用域

### 8. 序列化测试
- 字节序列化：正确的字节编码
- 校验和计算：自动校验和
- 随机值填充：随机端口、ID 等
- 变体展开：笛卡尔积

### 9. 集成测试
- 示例文件解析：所有 .pkt 和 .pktl 文件
- LSP 功能：诊断、补全、悬停
- 引擎功能：分析、hex、pcap

## 测试方法

1. **单元测试**：每个模块独立测试
2. **集成测试**：跨模块功能测试
3. **端到端测试**：完整工作流测试
4. **回归测试**：确保新功能不破坏旧功能

## 测试环境

- 依赖：packet-dsl crate
- 工具：cargo test, pktlang_tests
- 示例文件：examples/ 目录
- 测试数据：pktlang_tests/fixtures/

## 测试用例设计原则

1. **边界值测试**：测试边界条件
2. **错误路径测试**：测试错误处理
3. **性能测试**：测试解析和序列化性能
4. **兼容性测试**：测试向后兼容性
5. **文档一致性测试**：测试文档与实现的一致性

## 测试执行计划

1. 运行现有测试：`cargo test -p packet-dsl`
2. 运行 pktlang_tests：`python3 pktlang_tests/*.py`
3. 运行示例文件测试：`prping engine examples/`
4. 运行 LSP 测试：`prping engine --lsp`
5. 运行性能测试：解析和序列化基准测试

## 测试报告

生成测试报告，包括：
- 测试覆盖率
- 通过/失败统计
- 性能指标
- 已知问题和限制
