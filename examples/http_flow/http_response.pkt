# HTTP 流程步骤 2：HTTP 200 OK 响应
# 学习 HTTP 协议的响应格式
#
# HTTP 响应结构：
#   - 状态行：HTTP版本 状态码 原因短语\r\n
#   - 响应头部：Content-Type, Content-Length, Date 等
#   - 空行：\r\n（头部结束标志）
#   - 响应体：实际内容（HTML/JSON/图片等）
#
# 常见 Content-Type：
#   text/html               - HTML 文本
#   application/json        - JSON 数据
#   application/xml         - XML 数据
#   image/png               - PNG 图片
#   application/octet-stream - 二进制流
#
# 注意：真实场景中，响应由 Web 服务器自动生成，
# 这里构造的是模拟响应，用于理解协议结构

# 构造 HTTP 200 OK 响应
resp = http(start_line="HTTP/1.1 200 OK",
            headers=["Content-Type: text/html", "Content-Length: 36", "Connection: keep-alive"],
            body="<html><body>Hello</body></html>")

# 封装到 TCP（源和目标端口互换）
use(resp) |> tcp(sport=params("port", 80), dport=40000,
                 seq=global("sseq", 0x2000), ack=global("cseq", 0x1000),
                 flags=bor(psh(), ack()))
          |> ipv4(src=params("ip", "127.0.0.1"), dst="127.0.0.1")

export:
- resp
