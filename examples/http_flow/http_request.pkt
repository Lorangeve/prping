# HTTP 流程步骤 1：HTTP GET 请求
# 学习 HTTP 协议的请求格式
#
# HTTP (HyperText Transfer Protocol) 是应用层协议：
#   - 请求格式：方法 路径 HTTP版本\r\n 头部\r\n\r\n 体
#   - 响应格式：HTTP版本 状态码 原因\r\n 头部\r\n\r\n 体
#
# 常见 HTTP 方法：
#   GET     - 获取资源（最常用）
#   POST    - 提交数据
#   PUT     - 更新资源
#   DELETE  - 删除资源
#   HEAD    - 获取头部信息（无体）
#
# 常见状态码：
#   200 OK              - 请求成功
#   301 Moved Permanently - 永久重定向
#   302 Found           - 临时重定向
#   404 Not Found       - 资源不存在
#   500 Internal Server Error - 服务器内部错误
#
# 运行：prping packet http_request 127.0.0.1:80 --wait 1
# 目标地址可注入：-p ip=127.0.0.1,port=80

# 构造 HTTP GET 请求
# start_line: GET /index.html HTTP/1.1
# headers: Host 头部是必需的，Connection: keep-alive 表示保持连接
req = http(start_line="GET /index.html HTTP/1.1",
           headers=["Host: example.com", "Connection: keep-alive", "Accept: text/html"],
           body="")

# 封装到 TCP（端口 80 是 HTTP 默认端口）
use(req) |> tcp(sport=40000, dport=params("port", 80),
                seq=global("cseq", 0x1000), flags=bor(psh(), ack()))
         |> ipv4(src="127.0.0.1", dst=params("ip", "127.0.0.1"))

export:
- req
