# server 配方 · 第 1 组步骤 2（触发发包）：构造 SYN-ACK 发回请求方（三次握手第 2 段）。
# 字段全部来自 listen_syn 步骤的 extract（global）：
#   r_sport  = 请求的 tcp.sport（客户端端口）→ SYN-ACK 的 dport；
#   r_seq    = 请求的 tcp.seq → ack = r_seq + 1。**确认号 = 对方 seq + 1** 是三次握手
#              的数值核心："你的第 seq 号字节（SYN 本身占 1 个序号）我收到了，
#              下一个期望 seq+1"——算术 + 直接作用在 extract 出的整数值上；
#   r_src_ip（请求方 IP）→ dst；r_dst_ip（本机 IP，请求帧的 dst）→ src，地址整体互换。
# seq=0x2000 为服务端固定 ISN（初始序号）：固定值便于教学对照（README 时序表逐项
# 对账；client 侧 syn.pkt 同时演示 extract 动态提取服务端 ISN 的真实握手动作）。
# 真实内核的 ISN 随机且与时钟相关，教学场景固定才能把数字讲透——真实场景应随机化。
#
# 裸 IP 外层（无 eth 层）+ raw: true：有 tcp 传输层的发送**必须显式 raw**（否则引擎
# 走 TCP 载荷建连，变成真实 connect）；裸 IP 经内核 IP 栈路由注入（Linux
# IPPROTO_RAW+IP_HDRINCL），回环与局域网都无需 MAC 解析；tcp/ip 校验和由序列化器
# 自动重算。位常量 syn()/ack() 来自 eng_lib（元件/变量名勿用 syn/ack/fin 等位常量
# 短名，会遮蔽值函数——见 eng_lib/bytes.pkt）。
# global 缺省值仅用于独立解析/展示（prping engine 单跑本文件时的占位，取时序表教学
# 值 → 单跑展示 ack=0x1001）；配方运行时由 listen_syn 步骤 extract 注入真实匹配帧字段。
payload = raw(bytes="")
use(payload) |> tcp(sport=80, dport=global("r_sport", 40000), seq=0x2000, ack=global("r_seq", 0x1000) + 1, flags=bor(syn(), ack()), window=65535) |> ipv4(src=global("r_dst_ip", "127.0.0.1"), dst=global("r_src_ip", "127.0.0.1"))
