# server 配方 · 第 1 组步骤 2（触发发包）：用 listen_dns.pkt 命中后 extract 的
# global 构造**带 A 记录的 DNS 应答**，发回查询方：
#   global.tid   = 查询的 dns.id（reply.dns.id 来源）——应答必须回显同一 id，
#                  客户端靠 id 配对请求/应答（RFC 1035 §4.1.1）；
#   global.cip   = 查询方 IP（reply.peer.ip 来源——UDP 数据报监听只收到载荷，
#                  没有 udp/ip 头，对端地址来自 socket peer）；
#   global.cport = 查询方的 UDP 源端口（reply.peer.port 来源，同上）。
#                  注意这是**内核 socket 的真实源端口**：payload 模式发包走普通
#                  UDP socket，端口由内核临时分配，包头里写的 sport=12345 只是
#                  载荷字节的一部分，不代表对端真实端口——所以回包端口必须用
#                  reply.peer.port，不能猜。
# 应答字段：id 回显、flags=0x8180（QR=1 应答 + RD=1 + RA=1 递归可用）、
#   questions 回显 + answers 一条 A 记录（列表元组：名字/类型/类/TTL/地址，
#   ip4(...) 生成 4 字节记录数据，rdlength 自动计算）。
q = dns(id=global("tid"), flags=0x8180, questions=["example.com"], answers=[["example.com", 1, 1, 300, ip4("93.184.216.34")]])
full = use(q) |> udp(sport=5533, dport=global("cport")) |> ipv4(dst=global("cip")) |> eth()

export:
- full
