# UDP mock · 服务端配方（server.pktl）：一个 pktl 就是一个「按端口分派两种
# 应用协议」的 UDP 服务端——第 1 阶段 DNS（:5533），第 2 阶段 VNC 横幅（:5900）。
#
# 这里**两个 serve 阶段**顺序排列（各 `max: 1`）：为什么不分派成一条多规则
# serve 阶段？两条规则**监听方式不同**（第 1 条 UDP 数据报监听、第 2 条链路层
# `raw: true`——同阶段混用报错），且端口不同（:5533 / :5900，单 socket 无法
# 同时绑定两个端口），故保持顺序两阶段——先服务完 DNS 请求
# （命中 → 回包 → 第 1 阶段收工），再进入 VNC 阶段监听；client 两步之间本就带
# delay 间隔，语义等价。
#
# 第 1 阶段：listen_dns.pkt——有 udp 传输层 → 自动选 **UDP 数据报监听**（绑定
#   :5533，单阶段实验可免 root），sniffer 匹配 DNS 查询（flags=0x0100），命中后
#   extract：tid=查询 id；cip/cport=查询方 IP/端口（reply.peer.*，来自 socket
#   ——UDP 载荷里没有地址头）。handler 的 dns_reply.pkt 用 global 构造带 A 记录
#   的应答发回查询方。
# 第 2 阶段：listen_vnc.pkt（raw: true = **链路层监听**）——RFB 横幅是裸文本，
#   UDP 数据报监听里反解不出任何层、无法匹配（实测 ignored，见 listen_vnc.pkt
#   的详细说明）；链路层监听收完整帧，udp/raw 层齐全才能做「端口 + 内容」跨层
#   匹配。链路层监听没有 reply.peer.*（无 socket 对端），对端地址从帧内取：
#   cip2=reply.ipv4.src、cport2=reply.udp.sport。handler 的 vnc_banner.pkt 用
#   global 回横幅（普通 UDP socket 发送）。
#
# 注意：client 步骤 2 带 `delay: 0.5`，给本配方「第 1 阶段收工 → 第 2 阶段
# 监听就绪」留出时间——回环上 client 全程亚毫秒，不加 delay 会在 :5900 监听就绪
# 前就把横幅发出去（错过 = 第 2 阶段永远等不到，见 icmp_mock README 的同款经验）。
#
# 运行（需 root/管理员：第 2 阶段是链路层监听，抓帧要 cap_net_raw；第 1 阶段
# DNS 单独跑可免 root，见 listen_dns.pkt 注释）：
#   sudo prping packet examples/udp_mock/server.pktl
# 客户端（另一终端，全程免 root，见 client.pktl）：
#   prping packet examples/udp_mock/client.pktl
global:
- tid
- cip
- cport
- cip2
- cport2

recipe:
# ── 第 1 阶段：DNS 查询监听（UDP 数据报 :5533）→ extract → 触发发 A 记录应答 ──
- serve:
  max: 1
  rules:
  - packet: listen_dns.pkt
    extract:
    - name: tid
      from: reply.dns.id
      as: int
    - name: cip
      from: reply.peer.ip
      as: str
    - name: cport
      from: reply.peer.port
      as: int
    handler:
    - packet: dns_reply.pkt
# ── 第 2 阶段：VNC 横幅监听（链路层，raw: true）→ extract 帧内对端 → 回横幅 ──
- serve:
  max: 1
  rules:
  - packet: listen_vnc.pkt
    raw: true
    extract:
    - name: cip2
      from: reply.ipv4.src
      as: str
    - name: cport2
      from: reply.udp.sport
      as: int
    handler:
    - packet: vnc_banner.pkt
