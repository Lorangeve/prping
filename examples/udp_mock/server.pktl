# UDP mock · 服务端配方（server.pktl）：一个 pktl 就是一个「按端口分派两种
# 应用协议」的 UDP 服务端——第 1 组 DNS（:5533），第 2 组 VNC 横幅（:5900）。
#
# 配方显式列出**多组**监听+应答步骤（pktl 可包含任意多包）：每组 = 一次
# listen 命中（持续监听直到匹配）→ extract 写 global → 触发发包应答。
# 两组依次执行：先服务完 DNS 请求（命中 → 回包），再进入 VNC 组监听——
# 正好对应 client.pktl 的两步请求；要服务更多次就再写几组。
#
# 每组步骤：
#   第 1 组：listen_dns.pkt（wait: 无值 = 持续监听）——有 udp 传输层 → 自动选
#           **UDP 数据报监听**（绑定 :5533，单组实验可免 root），sniffer 匹配
#           DNS 查询（flags=0x0100），命中后 extract：tid=查询 id；cip/cport=
#           查询方 IP/端口（reply.peer.*，来自 socket——UDP 载荷里没有地址头）。
#           dns_reply.pkt 用 global 构造带 A 记录的应答发回查询方。
#   第 2 组：listen_vnc.pkt（wait: 无值 + raw: true = **链路层监听**）——RFB
#           横幅是裸文本，UDP 数据报监听里反解不出任何层、无法匹配（实测
#           ignored，见 listen_vnc.pkt 的详细说明）；链路层监听收完整帧，
#           udp/raw 层齐全才能做「端口 + 内容」跨层匹配。链路层监听没有
#           reply.peer.*（无 socket 对端），对端地址从帧内取：
#           cip2=reply.ipv4.src、cport2=reply.udp.sport。
#           vnc_banner.pkt 用 global 回横幅（普通 UDP socket 发送）。
#
# 注意：client 步骤 2 带 `delay: 0.5`，给本配方留出「关第 1 组监听 → 开第 2 组
# 监听」的时间——回环上 client 全程亚毫秒，不加 delay 会在 :5900 监听就绪前
# 就把横幅发出去（错过 = 第 2 组永远等不到，见 icmp_mock README 的同款经验）。
#
# 运行（需 root/管理员：第 2 组是链路层监听，抓帧要 cap_net_raw；第 1 组
# DNS 单独跑可免 root，见 listen_dns.pkt 注释）：
#   sudo prping packet examples/udp_mock/server.pktl
# 客户端（另一终端，全程免 root，见 client.pktl）：
#   prping packet examples/udp_mock/client.pktl
global:
- tid
- cip
- cport
- cip2
- cport2

recipe:
# ── 第 1 组：DNS 查询监听 → extract → 触发发 A 记录应答（client 步骤 1）──
- packet: listen_dns.pkt
  wait:
  extract:
  - name: tid
    from: reply.dns.id
    as: int
  - name: cip
    from: reply.peer.ip
    as: str
  - name: cport
    from: reply.peer.port
    as: int
- packet: dns_reply.pkt
# ── 第 2 组：VNC 横幅监听（链路层，raw: true）→ extract 帧内对端 → 回横幅 ──
- packet: listen_vnc.pkt
  wait:
  raw: true
  extract:
  - name: cip2
    from: reply.ipv4.src
    as: str
  - name: cport2
    from: reply.udp.sport
    as: int
- packet: vnc_banner.pkt
