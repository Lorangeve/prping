# DNS 流程步骤 2：DNS 查询响应
# 学习 DNS 协议的应答机制
#
# DNS 响应包含查询结果：
#   - 应答区：包含查询结果的资源记录
#   - 授权区：包含权威域名服务器信息
#   - 附加区：包含额外有用信息（如 IP 地址）
#
# DNS 压缩机制：
#   - 为了节省空间，DNS 报文使用压缩指针
#   - 压缩指针格式：0xC0xx（高 2 位为 11，低 14 位为偏移量）
#   - 指向报文中前面出现过的域名
#
# 注意：真实场景中，响应由 DNS 服务器自动生成，
# 这里构造的是模拟响应，用于理解协议结构

# 构造 DNS 查询响应
# id=0x5678 (与查询相同的事务 ID)
# flags=0x8180 (QR=1 应答, RD=1 期望递归, RA=1 支持递归)
# answers: A 记录，IP 地址 93.184.216.34 (example.com 的实际 IP)
response = dns(id=global("dns_id", 0x5678), flags=0x8180,
               questions=["www.example.com"],
               answers=[["www.example.com", 1, 1, 3600, ip4("93.184.216.34")]])

# 封装到 UDP（源和目标端口互换）
use(response) |> udp(sport=53, dport=12345)
              |> ipv4(src=params("ip", "127.0.0.1"), dst="127.0.0.1")

export:
- response
