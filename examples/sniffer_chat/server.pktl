# 服务端配方（server.pktl）：serve 监听分派 + extract 取值 + 触发回显。
#
# `serve:` 阶段（阻塞至收工）：单条规则监听 :55353（UDP 数据报监听，按
# listen.pkt 包内 udp dport 推导），sniffer 匹配 DNS 查询（and/not 组合规则，
# 见 listen.pkt）；`max: 2` = 逐轮服务两个查询（对应 client.pktl 的两步查询），
# 每轮：命中 → extract 写 global（tid=查询 id、cip/cport=查询方 IP/端口，
# reply.peer.* 来自 socket）→ handler 用 echo.pkt 重建查询（同 id）发回查询方。
# global 跨轮保留、每轮 extract 覆盖。
#
# 注意：每步发包用独立 socket（源端口不同）——回显走 global 里当轮的对端地址，
# 所以多轮共用一条规则即可。要服务更多查询：调大 `max:`；`max: -1` = 无限
# 服务，Ctrl+C 随时优雅收工（打印统计、退出码 0）。
#
# 运行（终端 1）：prping packet examples/sniffer_chat/server.pktl
# 客户端（终端 2）：prping packet examples/sniffer_chat/client.pktl 127.0.0.1:55353
global:
- tid
- cip
- cport

recipe:
- serve:
  max: 2
  rules:
  - packet: listen.pkt
    extract:
    - name: tid
      from: reply.dns.id
      as: int
    - name: cip
      from: reply.peer.ip
      as: str
    - name: cport
      from: reply.peer.port
      as: int
    handler:
    - packet: echo.pkt
