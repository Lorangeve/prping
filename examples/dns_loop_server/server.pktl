# 无限服务端配方（serve 阶段 + until 收工）：连续服务多个请求，直到停服包到达。
#
# `serve:` 阶段（阻塞至收工）：不写 `max:` = 无限服务（也可写 `max: 10`
# 限轮数，负数 = 无限的显式写法；字段空值非法——要么不写、要么写值）。单条规则
# 逐轮服务：命中 DNS 查询 → extract 写 global（tid=查询 id、cport=查询方端口）
# → handler 回应，global 跨轮保留、每轮 extract 覆盖。
#
# `until:` 谓词（sniffer 同款语法，可 and/or/not）——收到的包命中任一谓词，
# 本轮结束后收工：
#   - match dns(flags=0x8180)
# 停服包不是查询（sniffer flags=0x0100 不匹配），所以它只会触发 until——
# 本轮结束即收工、不回应（客户端发完停服包即看到服务端退出、退出码 0）。
# 阶段内 Ctrl+C 同样优雅收工（打印统计、退出码 0）。
#
# 注意：每轮回包走 UDP 载荷发送，发送路径有 ~1s 回读窗口——窗口内到达的下一条
# 查询会被丢弃（客户端按常规 DNS 重试即可命中下一轮监听）。
#
# 运行（终端 1）：prping packet examples/dns_loop_server/server.pktl
# 客户端（终端 2）：
#   prping packet --wait 2 examples/dns_trigger/client.pkt 127.0.0.1:55354
#   ……可连续多次查询，同一服务进程逐个应答……
# 停服（终端 2，发一个 flags=0x8180 的包）：
#   prping packet --wait 1 --params sid=0x8180 examples/dns_loop_server/shutdown.pkt 127.0.0.1:55354
global:
- tid
- cport

recipe:
- serve:
  until:
  - match dns(flags=0x8180)
  rules:
  - packet: listen.pkt
    extract:
    - name: tid
      from: reply.dns.id
      as: int
    - name: cport
      from: reply.peer.port
      as: int
    handler:
    - packet: reply.pkt
