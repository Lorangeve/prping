# 无限监听服务（packet .pktl，serve 阶段 + until）：sniffer 匹配结果触发发包
#
# 服务端配方（server.pktl）是一个 `serve:` 阶段（不写 max = 无限轮），每轮：
#   规则监听 listen.pkt ——不发送，用本文件的 sniffer 匹配外部到达的 DNS 查询，
#             命中后 extract 匹配包字段（dns.id / 对端端口 peer.port）写 global；
#   handler —— 触发发包：用 global 构造 DNS 应答发回查询方；然后进入下一轮继续
#             服务下一个请求。
# 收到 `flags=0x8180` 的停服包（server.pktl 的 until: 谓词）→ 本轮结束即收工退出。
#
# 客户端是普通 `--wait` 单包（发查询 + 校验应答）。

# 监听规则：DNS 查询（QR=0）；dport=55354 用于推导监听绑定地址
# （服务端 `packet server.pktl` 不传 HOST:PORT，按包内最外层 udp dport 绑定）。
p = raw(bytes="")
use(p) |> udp(dport=55354) |> ipv4(dst="127.0.0.1") |> eth()

sniffer:
  - match dns(flags=0x0100)
