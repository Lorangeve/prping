# 门禁 mock · serve 规则监听源（阻塞监听至命中，配方编排见 server.pktl）。
# 有 udp 传输层 → 自动选 UDP 数据报监听（绑定 :55353，按最外层 udp dport 推导）。
#
# ⚠ 载体必须是**可反解的协议**：裸 UDP 数据报若反解不出任何注册协议，层栈为空
# （dissect 只剩 remaining，见 packet-dsl/src/dissect.rs 的 dissect_bare_app），
# sniffer 无层可匹配——所以本示例用 DNS 查询作载体，候选 PIN 藏在 qname 标签里
# （线上格式 "PIN-XXXX" 连续字节原样出现在 DNS 名字里）。
#
# 「口令校验点」就在这条 sniffer 规则里：dns 层的原始字节（完整查询报文）做
# contains 字节谓词——只有 qname 携带正确 PIN 的查询才命中，错误候选被静默忽略
# （服务端继续监听），正确候选命中后 extract 取值、触发 grant.pkt 回包。
# 把 contains(...) 里的字面量改掉 = 换口令。
# （sniffer 值只接受字面量/值表达式，监听模式没有发包字段可引用。）
#
# 包定义本身仅用于推导监听端口与文档说明；实际匹配完全由 sniffer 决定。
q = dns(id=0x1337, flags=0x0100, questions=["PIN-0000.lab"])
use(q) |> udp(dport=55353) |> ipv4(dst="127.0.0.1") |> eth()

sniffer:
  - match dns(contains("PIN-4242"))
