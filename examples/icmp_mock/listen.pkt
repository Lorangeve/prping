# server 配方 · serve 规则监听源：链路层监听（包内无 udp/tcp 自动选）——匹配
# ICMP echo request（type=8）。监听打开全接口（--iface 可指定网卡）；命中后执行
# handler（触发发包）。
#
# 这里不需要包定义推导监听端口（链路层监听无端口），包体仅用于占位/文档；
# 实际匹配完全由 sniffer 规则决定。
p = raw(bytes="")
use(p) |> icmp(type=8) |> ipv4()

sniffer:
  - match icmp(type=8)
