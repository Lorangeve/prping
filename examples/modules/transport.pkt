# 模块系统 demo 第 2 层：传输层（import 应用层元件，组合后导出）
# 运行：prping --eng examples/modules/transport.pkt

import app { http_get, dns_query }

web = use(http_get) |> tcp(sport=40000, dport=443)
dns = use(dns_query) |> udp(dport=53)

export:
- web
- dns
