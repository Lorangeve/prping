# TCP 数据传输 mock · 服务端配方（server.pktl）：一个 pktl 就是一个「数据确认服务」。
#
# `serve:` 阶段（阻塞至收工）：单条规则逐轮服务——`max: 2` = 服务两轮（对应
# client.pktl 的两步数据发送），每轮：命中 → extract 写 global → handler 触发
# 发包纯 ACK 确认段；global 跨轮保留、每轮 extract 覆盖。
#
# 规则监听源 listen.pkt 只有 ipv4（无传输层）→ 链路层监听完整帧，sniffer 匹配
# PSH|ACK（数据段；纯 ACK/RST 不误命中，见 listen.pkt 注释），命中后 extract
# 匹配帧字段写 global：请求方端口/序列号（确认号推导用）+ 双向 IP。handler 的
# tcp_ack.pkt 用 global 构造纯 ACK（seq=0x2000 固定，ack = 本轮 r_seq + 45 =
# 数据段 seq + 载荷字节数）发回请求方（raw: true：有 tcp 传输层，必须显式 raw
# 才不会走 TCP 载荷建连）。
#
# 数值对照（client 两步，载荷均 45 字节，见 tcp_ack.pkt 的逐字计算）：
#   第 1 轮：收到 seq=0x1000 的数据段 → 回 ack=0x1000+45=0x102D 的纯 ACK
#   第 2 轮：收到 seq=0x102D 的数据段 → 回 ack=0x102D+45=0x105A 的纯 ACK
# 两轮共用一条规则（同一份 listen.pkt/tcp_ack.pkt）：extract 每轮重写同一组
# global（后写覆盖先写），tcp_ack.pkt 的 ack=global("r_seq")+45 自动适配每轮的
# seq——配方复用的典型形态（与 icmp_mock 的 reply.pkt 同思路）。
#
# 注意：client 步骤 2 带 `delay: 0.5`——serve 的轮次是串行的（命中 → handler
# 回包 → 下一轮监听就绪），回环上 client 亚毫秒跑完两步，不加 delay 第二个数据
# 段可能落在下一轮就绪前的间隙里被错过。
#
# 要服务更多数据段：调大 `max:`；`max: -1` = 无限服务，Ctrl+C 随时优雅收工。
#
# 运行（需 root；Linux AF_PACKET / macOS·Win libpcap 链路层抓帧）：
#   sudo prping packet examples/tcp_data_mock/server.pktl
# 客户端（另一终端，见 client.pktl 注释）：
#   sudo prping packet examples/tcp_data_mock/client.pktl
global:
- r_sport
- r_seq
- r_src_ip
- r_dst_ip

recipe:
- serve:
  max: 2
  rules:
  - packet: listen.pkt
    extract:
    - name: r_sport
      from: reply.tcp.sport
      as: int
    - name: r_seq
      from: reply.tcp.seq
      as: int
    - name: r_src_ip
      from: reply.ipv4.src
      as: str
    - name: r_dst_ip
      from: reply.ipv4.dst
      as: str
    handler:
    - packet: tcp_ack.pkt
      raw: true
