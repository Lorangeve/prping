# ARP mock · 客户端配方（client.pktl）：两步发包模拟——配方可包含任意多包。
#
# 步骤 1：发 ARP 请求（广播问「谁是 127.0.0.1」，tpa 可 -p ip= 注入换目标）→
#         `wait: 2` 校验 mock 服务端的 ARP 应答（sniffer 匹配 op="reply" +
#         服务端固定 MAC，见 arp_request.pkt 注释的双重过滤）→ extract
#         reply.arp.sha 写 global.rmac（从应答学习对方 MAC——协议栈的核心动作，
#         本示例单步用不到，纯演示 extract 复用入口）。
# 步骤 2：`delay: 0.5` 后发 gratuitous ARP（free_arp.pkt，纯发送无 wait）。
#         delay 的用意沿用 icmp_mock 的经验：server 每命中一次都要重新打开下一轮
#         抓包（设备枚举+绑定），回环上 client 亚毫秒就跑完，不加 delay 会在
#         server 下一轮监听就绪前就发出下一个包、错过服务；本例步骤 2 虽无
#         wait 期待，仍保留 delay 把两步在时间上分开，抓包观察更清晰。
#
# 运行（需 root；与 server.pktl 同机两个终端；缺省走 lo，AF_PACKET 按帧内
# 目的 MAC 直发）：
#   sudo prping packet examples/arp_mock/client.pktl
#   打真实网段邻居：-p ip=<邻居 IP>（见 arp_request.pkt；真实网段会收到真实
#   设备的应答，sniffer 固定 MAC 过滤保证只命中 mock 应答）
global:
- rmac

recipe:
- packet: arp_request.pkt
  raw: true
  wait: 2
  extract:
  - name: rmac
    from: reply.arp.sha
    as: str
- packet: free_arp.pkt
  raw: true
  delay: 0.5
