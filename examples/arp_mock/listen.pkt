# arp_mock · server 配方 · 步骤 1：链路层持续监听（`wait:` 无值 + 包内无 udp/tcp
# 传输层 → 自动选链路层监听完整帧，AF_PACKET 收完整 eth+arp 帧，需 root；
# 缺省监听全接口，`--iface` 可指定网卡）。
#
# 匹配规则：ARP 请求（op="request"）。**包体仅用于占位/文档**（表达"我在等
# eth+arp 帧"），实际匹配完全由 sniffer 规则决定——收到的帧反解出 eth/arp 层后
# 逐项比对规则。只匹配 request 不匹配 reply，所以本配方自己刚回的应答帧不会被
# 当成新请求（天然防自触发）。
#
# ── sniffer 字面量验证结论（写 ARP 规则前先看，免得再踩坑）────────────────
# arp 层可匹配字段：op / sha / spa / tha / tpa（matchpred.rs::field_names）。
# op 的反解值是 ArpOp 的 Display 字符串——"request" / "reply"（ir.rs::ArpOp），
# sniffer 里必须写**带引号的字符串字面量**：op="request" / op="reply"。
# 数值字面量 op=1 / op=2 过不了字面量类型检查（("arp","op") 在 matchpred.rs
# ::coerce_literal 的 is_str 名单里，整数进字符串位直接报错）。
# 注意分工：**构造**包时 op 用 eng_lib 位常量 request()/reply()（bytes.pkt
# L146/L148，= be16(1)/be16(2)，协议头里落成 1/2）；**匹配**帧时 op 用字符串
# "request"/"reply"（与反解展示值一致）。两处写法别混。
# ──────────────────────────────────────────────────────────────────────
p = arp(op=request())
use(p) |> eth()

sniffer:
  - match arp(op="request")
