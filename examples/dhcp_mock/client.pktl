# DHCP mock · 客户端配方（client.pktl）：两步发包模拟 DORA 的客户端两步。
#
# 步骤 1：发 Discover（op=1，xid=0x12345678，chaddr=00:11:22:33:44:55）→
#         wait: 2 校验 server 配方的 Offer（sniffer：op=2 + udp 67→68，见
#         dhcp_discover.pkt）→ extract reply.ipv4.src（Offer 的 IP 源地址，
#         即服务端地址）写 global.sip。
# 步骤 2：delay: 0.5 后发 Request（单播给 global.sip——extract 复用：前一步
#         从回包学到的服务端地址，这一步直接用；真实 Request 应广播，见
#         dhcp_request.pkt 说明）→ wait: 2 校验 Ack（同 sniffer 规则）。
#
# 为什么步骤 2 要 delay: 0.5：server 每命中一次都要重新打开下一轮抓包
# （AF_PACKET socket 重开），回环上 client 全程 <1ms 就跑完，不加 delay 会在
# server 下一轮监听就绪前就错过 Request（经验同 icmp_mock「为什么 seq 是
# 1001/1002」一节）。
#
# 为什么没有从回包 extract DHCP 字段：引擎没有 dhcp proto 层，Offer/Ack 的
# 载荷只能反解成整体 raw 层（回包能取的就是 reply.raw.bytes 一整段），取不到
# yiaddr/xid 等字段——这正是本 mock 全用固定值（xid/chaddr/yiaddr 预置数组）
# 的根因，见 listen.pkt 的反解验证结论与各 .pkt 文件头注。
#
# 所有发包步骤显式 raw: true：包内有 udp 传输层，不写 raw 会走普通 UDP socket
# 只发载荷——伪造的 sport=68、IP 头 0.0.0.0 源地址、校验和全部丢失，DHCP 的
# 传输层语义就没了。
#
# 运行（需 root：raw 发送走 IPPROTO_RAW+IP_HDRINCL，抓回包走 AF_PACKET；
# 与 server.pktl 同机两个终端）：
#   sudo prping packet examples/dhcp_mock/client.pktl
# 目标默认 127.0.0.1（params 缺省）；换目标用 --params ip=<地址>（记得同步
# 改成对应网络，mock 的封装才是端到端自洽的）。
global:
- sip

recipe:
- packet: dhcp_discover.pkt
  raw: true
  wait: 2
  extract:
  - name: sip
    from: reply.ipv4.src
    as: str
- packet: dhcp_request.pkt
  raw: true
  wait: 2
  delay: 0.5
