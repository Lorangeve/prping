# server 配方 · 步骤 2（触发发包）：用 wait 监听步骤 extract 的 global 构造 DNS
# 应答，发回查询方：
#   global.tid   = 查询的 dns.id（reply.dns.id 来源）
#   global.cip   = 查询方 IP（reply.peer.ip 来源——UDP 监听载荷里没有 udp 头，
#                  对端地址来自 socket peer）
#   global.cport = 查询方的 UDP 源端口（reply.peer.port 来源）
# 应答：id 回显请求 id、flags=0x8180（QR+RD+RA）、回显问题 + 一条 A 记录答案。
q = dns(id=global("tid"), flags=0x8180, questions=["example.com"], answers=[["example.com", 1, 1, 300, ip4("93.184.216.34")]])
full = use(q) |> udp(sport=53, dport=global("cport")) |> ipv4(dst=global("cip")) |> eth()

export:
- full
