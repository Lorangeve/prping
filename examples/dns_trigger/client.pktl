# 客户端配方（client.pktl）：发 DNS 查询（id=0x4242）到服务端配方（wait 持续监听
# 触发发包），wait: 2 校验服务端触发的应答（id 与发包一致 + 应答位 flags=0x8180）。
#
# 运行（两个终端）：
#   终端 1（服务端）：prping packet examples/dns_trigger/server.pktl
#   终端 2（客户端）：prping packet examples/dns_trigger/client.pktl 127.0.0.1:55353
#     （端口可注入：-p port=55353）
recipe:
- packet: client.pkt
  wait: 2
