# 配方监听触发（packet .pktl，`wait: -1` = 持续监听）：sniffer 匹配结果触发发包
#
# 服务端是一个**配方**（server.pktl），不是独立监听进程：
#   步骤 1 `wait: -1` —— 不发送，用本文件的 sniffer 匹配外部到达的 DNS 查询，
#             命中后 extract 匹配包字段（dns.id / 对端端口 peer.port）写 global；
#   步骤 2 —— 触发发包：用 global 构造 DNS 应答，发回查询方。
# 客户端是普通 `--wait` 单包（发查询 + 校验应答）。

# 监听规则：DNS 查询（QR=0）；dport=55353 用于推导监听绑定地址
# （服务端 `packet server.pktl` 不传 HOST:PORT，按包内最外层 udp dport 绑定）。
p = raw(bytes="")
use(p) |> udp(dport=55353) |> ipv4(dst="127.0.0.1") |> eth()

sniffer:
  - match dns(flags=0x0100)
