# HTTP mock · 客户端配方（client.pktl）：两步发包模拟——配方可包含任意多包。
# 形态同构 examples/icmp_mock/client.pktl：发包步骤 + wait: N 校验应答。
#
# 步骤 1：发 HTTP GET（http_get.pkt，raw: true 链路层发送——tcp 传输层的包必须
#         显式 raw: true，否则会走"TCP 载荷"模式用内核建连，mock 没有真 TCP 栈
#         握不了手）→ wait: 2 校验 200 OK 应答（sniffer：TCP 四元组/确认号 +
#         http 内容判定，见 http_get.pkt 注释）。
# 步骤 2：delay: 0.5 —— server 的两个 serve 阶段串行（GET 阶段收工 → POST 阶段
#         开监听），回环上 client 亚毫秒就发完，不加 delay 会错过 POST 请求
#         （同 icmp_mock「为什么 seq 是 1001/1002」一节的经验）。再发 POST
#         （http_post.pkt，JSON body + Content-Length: 32）→ wait: 2 校验。
#
# 为什么客户端不 extract：ICMP mock 里客户端 extract 回包字段做"复用"演示；
# 这里 TCP 应答在 Linux 默认构建（无 pcap feature）下收不到（引擎 raw 发送路径的
# 回包 socket 只收 ICMP，见 README「平台差异」），extract 依赖回包会把示例变成
# 必失败步骤。跨步骤复用的教学点见 icmp_mock/dns_echo_listen。
#
# 运行（需 root；与 server.pktl 同机两个终端）：
#   sudo prping packet examples/http_mock/client.pktl
# 端口/地址注入（两边都要一致）：
#   sudo prping packet examples/http_mock/client.pktl -p port=8080 -p ip=192.168.1.10
global:
- name: cseq   # 客户端 TCP 序列号（固定 0x1000——ack 可预知的教学 mock；真实客户端随每字节推进）
  init: 0x1000

recipe:
- packet: http_get.pkt
  raw: true
  wait: 2
- packet: http_post.pkt
  raw: true
  wait: 2
  delay: 0.5
