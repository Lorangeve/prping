# server 配方 · 第 1 阶段 handler 步骤（触发发包）：构造 HTTP 200 OK 响应发回请求方。
# 字段全部来自规则 extract（global，匹配帧即"请求"）：
#   r_sport  = 请求 tcp.sport（客户端源端口 40000）→ 响应的 dport
#   r_seq    = 请求 tcp.seq（客户端 ISN 0x1000）   → 响应 ack 的基准
#   r_src_ip = 请求方 IP（reply.ipv4.src）         → 响应的 dst
#   r_dst_ip = 本机 IP（reply.ipv4.dst）           → 响应的 src
#
# HTTP 响应格式（对照 http_get.pkt 的请求格式）：
#   状态行：HTTP版本 状态码 原因短语\r\n  → "HTTP/1.1 200 OK"
#   响应头部：Content-Type / Content-Length / Connection ...
#   空行：\r\n
#   响应体："<html><body>Hello</body></html>"
#
# 长度常量计算（Content-Length = 体字节数，纯 ASCII 每字符 1 字节）：
#   <html> 6 + <body> 6 + Hello 5 + </body> 7 + </html> 7 = 31 字节
#   （引擎 http(...) 是纯拼接不自动补 Content-Length——手动声明并保持一致，
#     写错时浏览器/客户端按声明长度截断或挂起，这是 HTTP 最经典的实验点）
#
# ack 计算：ack = r_seq + 90（GET 请求字节数，逐行算式见 http_get.pkt 注释）。
#   TCP 确认号语义：ack = 已连续收到的最后一字节序号 + 1 = 请求 seq + 请求长度。
#   client 的 sniffer 用固定字面量 0x105A（0x1000+90）校验此值。
#
# TCP 序列号（固定值便于教学对照，真实服务端 ISN 随机、应 extract 后推进）：
#   seq=0x2000 = 服务端固定 ISN（配方标记，client sniffer 据此钉住应答帧）；
#   flags=bor(psh(), ack()) = PSH|ACK（0x18）：PSH 提示"数据交应用层"，ACK 表示
#   确认号有效——HTTP 响应帧的典型旗标组合。
#
# 发送：raw: true（client.pktl 步骤里）+ eth 外层 → AF_PACKET/pcap 链路层注入
# （默认 lo；需 root）。MAC 全零 = 回环 lo 的真实 MAC（同 http_get.pkt 说明）。
resp = http(start_line="HTTP/1.1 200 OK",
            headers=["Content-Type: text/html", "Content-Length: 31", "Connection: keep-alive"],
            body="<html><body>Hello</body></html>")
use(resp) |> tcp(sport=params("port", 80), dport=global("r_sport"),
                 seq=0x2000, ack=global("r_seq") + 90, flags=bor(psh(), ack()))
          |> ipv4(src=global("r_dst_ip"), dst=global("r_src_ip"))
          |> eth(src_mac="00:00:00:00:00:00", dst_mac="00:00:00:00:00:00")
