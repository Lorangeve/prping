# server 配方 · 第 1 组步骤 1：链路层持续监听（wait: 无值），匹配 HTTP GET 请求帧。
#
# 监听方式**自动选择**：本包不含 udp/tcp 传输层（p = raw 占位 + ipv4/eth 外壳）→
# 引擎选**链路层监听**完整帧（listen_raw_once，AF_PACKET/Npcap，需 root——
# 抓包权限）；命中后配方继续（触发 resp_get.pkt 发包）。
# ⚠ 包定义里千万不要加 tcp 层：监听方式按"包内有无 udp/tcp"自动判定，
#   加了会被当成 UDP 数据报监听（载荷反解）——链路层监听拿不到完整帧的 tcp/http 层。
# 包体仅用于占位/文档（实际匹配完全由 sniffer 规则决定，同 icmp_mock/listen.pkt）。
#
# 预期的帧（client 的 http_get.pkt 发出，回环 lo 上的真实形状）：
#   eth(dst=00:..:00, src=00:..:00, ethertype=0x0800)
#    └ ipv4(src=127.0.0.1, dst=127.0.0.1, proto=6/TCP)
#       └ tcp(sport=40000, dport=80, seq=0x1000, flags=PSH|ACK)
#          └ 载荷 = "GET /index.html HTTP/1.1\r\nHost: ...\r\n\r\n"（90 字节）
#
# http 层为什么能被反解出来：http proto 的分派规则（eng_lib/headers.pkt）
#   #[rule(or(tcp(dport=80), tcp(dport=8080)))]——TCP 目标端口 80/8080 的载荷
#   走快速分派路径，按 http 声明（start_line + hdr_line 头行重复 + 空行 + body）
#   反解出 http 层，请求行 "GET /index.html HTTP/1.1" 按空格拆为
#   method="GET" / path="/index.html" / version="HTTP/1.1"。
#   （规则未命中时还有内容识别兜底：载荷含 "HTTP/" 魔数即可反解——协议识别
#     以内容为准，端口只是提示。所以监听规则换 -p port= 注入的非标准端口照样命中。）
#
# sniffer 规则只用字面量（listen 无发包可引用，裸 Ident 构建期报错）：
#   http(method="GET") —— http 是 IR 语义层（method/path/version 字段可与
#   --eng 展示一致地匹配，见 matchpred.rs field_names）。内核对无监听 80 端口回的
#   RST 帧（flags=rst、载荷空）反解不出 http 层，永不误触发。
p = raw(bytes="")
use(p) |> ipv4() |> eth()

sniffer:
  - match http(method="GET")
