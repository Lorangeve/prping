# client 配方 · 步骤 2：发 HTTP POST 请求（JSON body + 自定义头；delay: 0.5 由 client.pktl 编排）。
#
# POST 与 GET 的差异（对照 http_get.pkt）：
#   - 请求行方法变为 POST，路径 /login；
#   - 有请求体：body 携带 JSON（application/json 是最常用的 POST 内容类型）；
#   - 必须声明 Content-Length（引擎 http(...) 是纯拼接，不自动补；真实客户端库
#     会自动计算补齐——此处手动声明，body 恰好 32 字节，见下方计算）。
#
# 请求字节数（决定服务端应答的 ack）：
#   请求行 "POST /login HTTP/1.1"                       20 + \r\n = 22
#   "Content-Type: application/json"                    30 + \r\n = 32
#   "User-Agent: prping/0.1"                            22 + \r\n = 24
#   "Content-Length: 32"                                18 + \r\n = 20
#   空行                                                 \r\n      =  2
#   body "{\"user\":\"admin\",\"pass\":\"123456\"}"                  = 32
#     （{ " u s e r " : " a d m i n " ,  " p a s s " : " 1 2 3 4 5 6 " }
#       1+6+1+7+1+6+1+8+1 = 32，ASCII JSON 每字符 1 字节）
#   合计  22+32+24+20+2+32 =                                  132 字节
#   服务端 200 OK 的 ack = 客户端 seq + 132 = 0x1000 + 132 = 0x1084
#   （已用引擎逐字节核验：engine http_post.pkt 的整帧 186 B = eth14+ip20+tcp20+载荷132）
#
# 固定值说明：sport=40000/seq=0x1000 与 GET 步骤相同——真实 TCP 会话里第二个请求
# 的 seq 应等于上一次交互推进后的值；mock 固定 cseq 是为了 ack 可预知（教学对照）。
# sniffer 两条规则同 http_get.pkt：① TCP 层（ack=0x1084 验证 POST 132 字节被消费）
# ② http 内容判定（version 落整条响应行）。
req = http(start_line="POST /login HTTP/1.1",
           headers=["Content-Type: application/json", "User-Agent: prping/0.1", "Content-Length: 32"],
           body="{\"user\":\"admin\",\"pass\":\"123456\"}")
# seq=global("cseq", 0x1000)：同 http_get.pkt（配方 init 为权威值，内联默认供单文件分析）。
use(req) |> tcp(sport=40000, dport=params("port", 80), seq=global("cseq", 0x1000),
                flags=bor(psh(), ack()))
         |> ipv4(src=params("sip", "127.0.0.1"), dst=params("ip", "127.0.0.1"))
         |> eth(src_mac="00:00:00:00:00:00", dst_mac="00:00:00:00:00:00")

sniffer:
  - match tcp(dport=40000, seq=0x2000, ack=0x1084, flags="psh,ack")
  - match http(version="HTTP/1.1 200 OK")
