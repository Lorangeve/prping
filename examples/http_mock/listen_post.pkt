# server 配方 · 第 2 阶段规则监听源：链路层监听，匹配 HTTP POST 请求帧。
# 结构与 listen_get.pkt 完全同构，只有两处不同：
#   1. sniffer 匹配 method="POST"（请求行 "POST /login HTTP/1.1" 拆出 method）；
#   2. extract 目标相同（r_sport/r_seq/r_src_ip/r_dst_ip），但本次命中的是
#      POST 帧——ack 推进 136 字节（见 resp_post.pkt / http_post.pkt 的字节计算）。
#
# 同样的注意点：包定义无 udp/tcp 层 → 自动选链路层监听（需 root）；两个 serve 阶段
# 切换（关第 1 阶段监听 → 开第 2 阶段）需要时间，client 步骤 2 的 delay: 0.5 就是
# 给这留的窗口（同 icmp_mock「为什么 seq 是 1001/1002」的经验）。
p = raw(bytes="")
use(p) |> ipv4() |> eth()

sniffer:
  - match http(method="POST")
